[build-system]
requires = ["setuptools>=70", "torch==2.11.0"]
build-backend = "setuptools.build_meta"

[project]
name = "montlok-rdt-cpu"
version = "0.1.0"
description = "Weight-compatible CPU inference runtime for RDT4quant Mamba-3 SISO checkpoints"
requires-python = ">=3.12"
license = {text = "Apache-2.0"}
dependencies = ["numpy>=2.2,<3", "torch==2.11.0"]
